export enum Collection {
  USERS = "users",
  GAMES = "games",
  ADMIN = "admin",
  ME = "me",
}

export enum GameStatus {
  PAST = "past",
  UPCOMING = "upcoming",
}

export enum Role {
  ADMIN = "admin",
}
